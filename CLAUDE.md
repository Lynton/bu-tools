@AGENTS.md

# Claude Code

`AGENTS.md` is the canonical shared instruction source. Add Claude-specific
instructions here only when an actual Claude Code mechanism requires them; do not
copy shared policy into this file.
